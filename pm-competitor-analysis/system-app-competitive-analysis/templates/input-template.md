# 输入模板

```text
目的：（可选：功能借鉴 / 差异化竞争 / 综合分析）
系统 APP：
功能模块：
调研主题：
指定竞品：（可选）
特别关注项：（可选）
```

如果用户省略字段，只能使用 `config/constants.yaml` 中明确配置的默认值，不得自行编造其他信息。地区、系统版本和输出格式不作为用户输入项；地区与版本在研究过程中作为证据属性记录，输出使用全局默认格式。
